# Streamlit Community Cloud / generic pip install entry point.
#
# Streamlit Cloud expects a requirements.txt at the repo root. The "real"
# dependency definitions live in pyproject.toml — this file mirrors the
# minimum runtime set the public dashboard needs.
#
# Local development uses `pip install -e .[dev,ui]` (full set incl. tests).
# The cloud deploy only needs the ui + reading-snapshots subset since it
# runs in JOBMARKET_PUBLIC_MODE=1 (no scrape, no LLM, no creds).

streamlit>=1.30
altair>=5.0
pandas>=2.0
streamlit-autorefresh>=1.0
psutil>=5.9
python-dotenv>=1.0
